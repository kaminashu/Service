<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <androidx.appcompat.widget.AppCompatButton
        android:id="@+id/forgroundId"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="ForgroundSErvice"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <androidx.appcompat.widget.AppCompatButton
        android:id="@+id/stopForground"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Stop forground"
        app:layout_constraintTop_toBottomOf="@id/forgroundId" />

    <androidx.appcompat.widget.AppCompatButton
        android:id="@+id/workerBtn"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="work service"
        app:layout_constraintTop_toBottomOf="@+id/jobServiceBtn" />

    <androidx.appcompat.widget.AppCompatButton
        android:id="@+id/jobServiceBtn"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Job service"
        app:layout_constraintTop_toBottomOf="@+id/stopForground" />
</androidx.constraintlayout.widget.ConstraintLayout>